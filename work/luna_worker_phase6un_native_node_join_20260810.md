# Phase 6UN — PS7331 native node reachability joins

Date: 2026-08-10 (Asia/Taipei). Scope is host-only static review of the preserved PS7331 source archive, kernel configuration, boot image metadata, DTS inventory, extracted manifests, and selected native/framework artifacts. No device node was opened; no ioctl, module load, proc/sysfs/debugfs write, probe, or device contact was performed.

## Result

The requested CMDQ, ION, and Amazon-LD joins are only partial. CMDQ and ION are selected by the preserved kernel configuration; Amazon-LD is also selected and registers a platform driver. The source archive establishes source entry points and several node/attribute modes, but does not contain the final Android `ueventd`/`file_contexts`/vendor-TE joins, a complete module/object manifest, or a native framework/HAL caller chain. Those edges remain `UNKNOWN` below.

| Surface | Source/config → shipped object/module | DT/init | Node mode/label | Caller | Possible effect | Conservative verdict |
|---|---|---|---|---|---|---|
| CMDQ/MDP | `cmdq_driver.c` creates the character device; `CONFIG_MTK_CMDQ=y`, `CONFIG_MTK_CMDQ_TAB=y`. Exact Image symbol/object provenance and `.ko` packaging: `UNKNOWN`. | OF match `mediatek,gce`; `device_create` at source lines 848–865. No Android init registration or matching trona-DTS node was preserved. | `/dev/mtk_cmdq` mode, ueventd rule, file-context type, and TE allow: `UNKNOWN`. Proc debug entries are 0440; source sysfs attrs are owner read/write. | No native framework/HAL or direct open/ioctl caller established in selected artifacts. | Async command/wait/readback and MDP register/display/DMA effects are possible; package/settings/HOME/privilege effect `UNKNOWN`. | Partial source/config + driver-match confirmation; not a reachability proof. |
| ION | `ion.c` plus MTK `ion_drv.c`; `CONFIG_ION=y`, `CONFIG_MTK_ION=y`. `ion_device_create` and platform registration are present; exact shipped object/module packaging: `UNKNOWN`. | No matching DT/compatible or Android init registration established; driver/source path is present but instance edge is `UNKNOWN`. | Prior saved metadata records `/dev/ion` 0666, `system:graphics`, `ion_device`; final ueventd/file_contexts/TE source is absent here. | Bounded selected APK/JAR/native scan found no direct client; GPU/native HAL caller and exact open/ioctl path `UNKNOWN`. | Allocation, mapping, sharing, import, sync, and custom ioctl can affect memory/DMA state; higher-level state effect `UNKNOWN`. | Partial source/config + saved metadata; final policy, DT/init, and caller remain `UNKNOWN`. |
| Amazon-LD | `amzn_ld.c`; `CONFIG_AMAZON_LD=y`, `CONFIG_AMAZON_LD_SWITCH=y`; exact Image symbol/object provenance `UNKNOWN`. | Driver matches `amzn,ld` and calls `platform_driver_register`; no matching `amzn,ld` node was found in the checked trona HVT DTS slice; init registration `UNKNOWN`. | Sysfs stores and module params are source-declared 0664. Sysfs has no ueventd node rule; final file-context/TE/domain allow and runtime label `UNKNOWN`. | No native framework/HAL or direct writer established. Saved shell audit found no user attrs and denied module parameters; no write performed. | Liquid/thermal/USB behavior and thresholds may change if instantiated and authorized; package/settings/HOME/privilege effect `UNKNOWN`. | Partial config/source + registration; DT instance, policy, and caller reachability `UNKNOWN`. |

## Evidence boundaries

- Input hashes: `platform.tar` SHA-256 `69c62d65a387e22b39678df8054e6cae12b4d95b8c5d9bf21ad53811491a7fdd`; preserved kernel config SHA-256 `eefb8db484f65e196a7bb401ae0165f434f08b13041aef6762917e284d013d04`; `boot.img` SHA-256 `cf12e5619d635ecf7927784a4ed15a254a96c1642d1db9e4ca6734b192d6fa1b`; selected extraction manifest SHA-256 `b098f85287a729d03cfac1ac6f767a3ba6a15ce8c65d7f9e581cbbed75448a74`.
- Primary anchors are recorded row-by-row in the companion CSV: CMDQ `cmdq_driver.c:52-53,816-824,848-865,894-898`, MDP `mdp_ioctl_ex.c:332-405,602-729`; ION `ion.c:1502-1658,1906-1956`, `ion_drv.c:703-705,832-872`; Amazon-LD `amzn_ld.c:27-28,618-738,878-896`; config lines 1247–1248, 3532–3535, and 3563–3564.
- The source archive is not a final Android system/vendor/product tree. No supplied artifact closed the exact `ueventd*.rc`, `file_contexts`, `seapp_contexts`, vendor policy allow, init action, native HAL/client, or shipped module edge for these three surfaces. Missing edges are intentionally `UNKNOWN`, not inferred from source mode, config selection, or policy absence.
- No row establishes a PackageManager, SettingsProvider, ActivityManager, HOME resolver, Fire Launcher, or privilege-transition sink.

The companion ledger is [luna_worker_phase6un_native_node_join_20260810.csv](luna_worker_phase6un_native_node_join_20260810.csv).
