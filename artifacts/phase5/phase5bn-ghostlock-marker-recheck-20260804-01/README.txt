Phase 5BN host-only GhostLock marker recheck

This directory records an independent rerun of the preserved source-marker
comparison and a read-only HTTP-header check of the two official Amazon source
archives. It does not invoke adb, fastboot, a bootloader, a device node, or an
exploit. It does not produce offsets, addresses, payloads, or binaries.

