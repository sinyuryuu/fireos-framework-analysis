Phase 5BO exact build-selected source marker comparison

The PS7330 input is the full-archive extracted
kernel/mediatek/mt8183/4.4/rtmutex.c, not the generic kernel/mediatek/4.4
member. The comparison is host-only and does not produce addresses, offsets,
payloads, or device commands.

