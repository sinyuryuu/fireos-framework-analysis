Phase 5Q Android CMDQ implementation review

Host-only derived artifact. It compares the recovered Fire v2/v3 source
excerpts with the already archived CMDQ-IOCTL-V3-COMPAT-T01 stdout. It does
not download, compile, install, or execute an Android PoC, and it does not
invoke adb, open a device node, or issue an ioctl.

The public AOSP implementation is referenced by URL in
findings/phase-5q-android-cmdq-implementation-review.md. Raw exploit source is
not copied into this repository.
