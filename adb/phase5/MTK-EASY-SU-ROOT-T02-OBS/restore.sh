#!/usr/bin/env bash
# This directory records a read-only observation; no device mutation exists
# to restore. Kept as an explicit no-op so the evidence contract is clear.
set -u
echo 'No-op: MTK-EASY-SU-ROOT-T02-OBS issued no device mutation.'
